{{!-- <!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Example App - Home</title>
</head>
<body>

    <!-- Uses built-in `if` helper. -->
  {{#if showTitle}}
    <h1>Home</h1>
    <h1></h1>
  {{/if}}

    <!-- Calls `foo` helper, overridden at render-level. -->
    <p>{{foo}}</p>

    <!-- Calls `bar` helper, defined at instance-level. -->
    <p>{{bar}}</p>

</body>
</html> --}}

<h1>{{title}}</h1>

 <div class="container2">
            <div class="box-nav d-flex justify-between">
                <a href="/AddSub" class="border-shadow">
                    <span class="text-gradient">New User <i class="fas fa-user"></i></span>
                </a>
            </div>
<h3>{{titlename}}</h3>


<table class="table">
  <thead>
    <tr>

      <th scope="col">#</th>
      <th scope="col">Subject Name</th>
      <th scope="col">Action</th>
      


    
    
    </tr>
  </thead>

  <tbody>
    {{!-- {{# each contents }}
        <div class="thumbnail">
            <div class="caption">
                <h3>{{ this.SubjectName }}</h3>
                <br>
                <small> {{ this.name}} </small>
                <br>
                <p class="description">{{ this.name }}</p>
            </div>
        </div>
{{/each}} --}}
    {{#each list_subject}}

    <tr>
    <th scope="row">1</th>

      <td>{{this.name}}</td>
      <td href="#" type ="button" class="btn btn-light btn-small"><i class="bi bi-eye"></i>View</td>
      <td>
      <a href="/update/{{this._id}}" type ="button" class="btn btn-light btn-small"><i class="bi bi-pencil"></i>Edit</a>


      </td>
      <td>
        {{!-- <form method="POST" action="/delSubject/{this._id}}">
    <div class="form-group">
      <input id="id" class="form-control" type="hidden" name="name" required="true" value={{category._id}}>
    </div>
    <button class="btn btn-danger" type="submit">Delete</button>
  </form> --}}
    {{!-- <a href="/delSubject/{{_id}}" type ="button" class="btn btn-light btn-small"></i>Delete</a>  --}}
  {{!-- <a href="/update/{{this._id}}"><i class="fa fa-edit" aria-hidden="true">nn</i></a> --}}

  <a href="/deltedata/{{this._id}}" class="btn btn-light btn-small" onclick="return confirm('Are you sure about removing this Subject record?')"><i class="text-danger fa fa-trash">Delete</i></a>


      </td>
    </tr>
    {{/each}}


    
  </tbody>
</table>


